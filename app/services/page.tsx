import Link from "next/link";import { ArrowRight } from "lucide-react";import { CTA,PageHero } from "@/components/Sections";import { services } from "@/content/site";import{createMetadata}from"@/lib/seo";
export const metadata=createMetadata({title:"Architecture and Construction Services in Islamabad",description:"Explore architectural design, construction, interiors, renovation and project management services in Islamabad.",path:"/services"});
export default function Services(){return <><PageHero eyebrow="Services" title="Design and construction, connected." text="Support at every stage—from architectural concepts and approvals to construction, finishing, interiors, renovation and site supervision."/><section className="dark-section"><div className="service-overview-grid">{services.map((s,i)=><article className="service-overview-card" key={s.slug}><s.icon/><p className="eyebrow">0{i+1} · {s.eyebrow}</p><h2>{s.title}</h2><p>{s.summary}</p><Link className="under-link light" href={`/services/${s.slug}`}>Explore service <ArrowRight/></Link></article>)}</div></section><CTA/></>}
