import { CTA,PageHero } from "@/components/Sections";import { ProjectCard } from "@/components/ProjectCard";import { projects } from "@/content/site";import{createMetadata}from"@/lib/seo";
export const metadata=createMetadata({title:"Construction and Interior Projects in Islamabad",description:"Explore Neo's residential, commercial, interior and renovation portfolio.",path:"/projects",noIndex:true});
export default function Projects(){return <><PageHero eyebrow="Selected projects" title="A portfolio made to be verified." text="This portfolio structure is ready for genuine project photography and details. Current names and images are clearly marked editorial placeholders."/><section className="content-section"><div className="container filter-row"><span>All work</span><span>Residential</span><span>Commercial</span><span>Interiors</span><span>Renovation</span><span>In progress</span></div><div className="container projects-page-grid">{projects.map((p,i)=><ProjectCard key={p.slug} project={p} index={i}/>)}</div></section><CTA/></>}
