import type { Metadata } from "next";import Image from "next/image";import {notFound} from "next/navigation";import {CTA,PageHero} from "@/components/Sections";import {projects} from "@/content/site";import{createMetadata}from"@/lib/seo";
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)return{};return createMetadata({title:`${p.title} — ${p.location}`,description:"Project case study awaiting verified company portfolio information.",path:`/projects/${p.slug}`,noIndex:true})}
export default async function Project({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)notFound();return <><PageHero eyebrow={`${p.type} · ${p.location}`} title={p.title} text="Editorial placeholder case study. Replace this copy and imagery with verified company project information before publishing."/><div className="project-detail-visual"><Image src="/images/neo-hero.png" fill priority alt="Editorial architectural image placeholder" style={{objectPosition:p.crop}}/></div><section className="content-section"><div className="container content-grid"><div><p className="eyebrow">Project story</p><h2>A structure ready for the real story.</h2></div><div className="prose"><p>Use this case study to explain the client brief, site context, design direction and Neo’s confirmed scope. Include verified challenges, decisions, construction progress and the completed outcome.</p><p>No project claims, dates, client details or testimonials have been fabricated for this page.</p></div></div><div className="container project-facts"><div><span>Location</span><b>{p.location}</b></div><div><span>Type</span><b>{p.type}</b></div><div><span>Area</span><b>{p.area}</b></div><div><span>Status</span><b>{p.status}</b></div></div></section><CTA/></>}
