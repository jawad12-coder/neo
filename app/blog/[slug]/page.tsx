import type{Metadata}from"next";import{notFound}from"next/navigation";import{CTA,PageHero}from"@/components/Sections";import{posts}from"@/content/site";import{createMetadata}from"@/lib/seo";
export function generateStaticParams(){return posts.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=posts.find(x=>x.slug===slug);if(!p)return{};return createMetadata({title:p.title,description:p.excerpt,path:`/blog/${p.slug}`,noIndex:true})}
export default async function Post({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=posts.find(x=>x.slug===slug);if(!p)notFound();return <><PageHero eyebrow={`${p.category} · Draft article`} title={p.title} text={p.excerpt}/><section className="content-section"><article className="container legal"><p className="eyebrow">Editorial placeholder · Technical review required</p><p>This article template is ready for verified, locally relevant content. Before publishing, have construction and engineering statements reviewed by an appropriately qualified professional.</p><h2>Begin with the scope</h2><p>Comparisons are only useful when inclusions, exclusions, specifications, quantities and responsibilities are written consistently. Avoid comparing headline figures without understanding what each figure covers.</p><h2>Document assumptions</h2><p>Record the site information, drawings, selected materials, decision responsibilities and programme assumptions behind a proposal. Clear documentation makes later conversations more productive.</p><h2>Ask project-specific questions</h2><p>Every site and project is different. Use general guidance to prepare questions, not as a substitute for a technical assessment, approval advice or signed project agreement.</p></article></section><CTA/></>}
