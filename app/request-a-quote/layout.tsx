import { createMetadata } from "@/lib/seo";

export const metadata = createMetadata({ title: "Request a Construction Quote in Islamabad", description: "Share your Islamabad construction, architectural, interior or renovation requirements with Neo.", path: "/request-a-quote" });
export default function QuoteLayout({ children }: { children: React.ReactNode }) { return children; }
